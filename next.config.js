/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["img.icons8.com", "https://img.freepik.com/free-photo/"],
  },
};

module.exports = nextConfig;
